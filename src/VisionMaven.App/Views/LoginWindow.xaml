<Window
    x:Class="VisionMaven.App.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    Title="登录"
    Width="380"
    Height="270"
    Background="{StaticResource BackgroundBrush}"
    FontFamily="{StaticResource PrimaryFontFamily}"
    FontSize="{StaticResource BodyFontSize}"
    Foreground="{StaticResource TextPrimaryBrush}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    prism:ViewModelLocator.AutoWireViewModel="True">

    <Border BorderBrush="{StaticResource BorderBrushToken}" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,18"
                FontSize="{StaticResource TitleFontSize}"
                FontWeight="SemiBold"
                Text="VisionMaven" />

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource FieldLabelText}" Text="账号" />
                <TextBox
                    Grid.Column="1"
                    Height="26"
                    VerticalContentAlignment="Center"
                    Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource FieldLabelText}" Text="密码" />
                <PasswordBox
                    x:Name="PasswordInput"
                    Grid.Column="1"
                    Height="26"
                    VerticalContentAlignment="Center"
                    PasswordChanged="OnPasswordChanged" />
            </Grid>

            <TextBlock
                Grid.Row="3"
                Margin="0,0,0,6"
                Foreground="{StaticResource DangerBrush}"
                Text="{Binding ErrorMessage}"
                TextWrapping="Wrap"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}" />

            <StackPanel
                Grid.Row="5"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Command="{Binding LoginCommand}"
                    Content="登录"
                    IsDefault="True"
                    Style="{StaticResource ActionButtonPrimary}" />
                <Button
                    Click="OnCancelClick"
                    Content="退出"
                    IsCancel="True"
                    Style="{StaticResource ActionButton}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
