<Window
    x:Class="VisionMaven.App.Views.TextInputWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{Binding Title}"
    Width="420"
    SizeToContent="Height"
    Background="{StaticResource BackgroundBrush}"
    FontFamily="{StaticResource PrimaryFontFamily}"
    FontSize="{StaticResource BodyFontSize}"
    Foreground="{StaticResource TextPrimaryBrush}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Border BorderBrush="{StaticResource BorderBrushToken}" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,14"
                FontSize="{StaticResource SubtitleFontSize}"
                FontWeight="SemiBold"
                Text="{Binding Title}" />

            <ItemsControl Grid.Row="1" ItemsSource="{Binding Fields}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource FieldLabelText}" Text="{Binding Label}" />
                            <TextBox
                                Grid.Column="1"
                                Height="26"
                                VerticalContentAlignment="Center"
                                Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel
                Grid.Row="2"
                Margin="0,14,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Click="OnConfirm"
                    Content="确定"
                    IsDefault="True"
                    Style="{StaticResource ActionButtonPrimary}" />
                <Button
                    Click="OnCancel"
                    Content="取消"
                    IsCancel="True"
                    Style="{StaticResource ActionButton}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
